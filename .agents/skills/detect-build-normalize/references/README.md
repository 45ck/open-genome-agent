    # Detect build and normalize references

    ## Typical inputs

    - `build is unknown or ambiguous`
- `contig naming style differs across tools`
- `annotation expects normalized variants`

    ## Typical outputs

    - `derived/normalized.vcf.gz`
- `normalization_notes.json`

    ## Notes

    This directory is for durable supporting references, not hidden prompt logic.
    Keep external assumptions documented and keep command examples deterministic.
