    # Nextflow runner references

    ## Typical inputs

    - `raw-read or heavy pipeline execution is explicitly requested`
- `the environment is ready for Nextflow`

    ## Typical outputs

    - `workflow_run.json`
- `pipeline_logs/`

    ## Notes

    This directory is for durable supporting references, not hidden prompt logic.
    Keep external assumptions documented and keep command examples deterministic.
