#!/bin/sh
# noslop pre-commit: fast gates (format, lint, spell)
set -e

if command -v noslop >/dev/null 2>&1; then
  noslop check --tier=fast --pack python --no-spell
else
  npm run format:check && npm run lint && npm run spell
fi
