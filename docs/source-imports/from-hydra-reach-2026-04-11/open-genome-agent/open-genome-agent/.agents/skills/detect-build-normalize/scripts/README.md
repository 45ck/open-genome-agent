# Scripts for detect-build-normalize

Put deterministic helper scripts for this skill here.

Suggested pattern:

- one script per stable task
- clear stdin/stdout contracts
- no silent network access
- write derived artifacts under `runs/<run_id>/`
