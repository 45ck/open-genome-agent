model = "gpt-5.4"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
project_root_markers = [".git", ".hg", ".sl"]

[features]
codex_hooks = true
multi_agent = true

[agents]
max_threads = 6
max_depth = 1
