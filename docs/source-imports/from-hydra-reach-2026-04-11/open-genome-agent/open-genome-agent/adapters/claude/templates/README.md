# Claude adapter templates

These files are rendered into:

- `CLAUDE.md`
- `.claude/settings.json`
- `.claude/skills/*`
- `.claude/agents/*`

The generated runtime bundle lives under `adapters/claude/dist/`.
