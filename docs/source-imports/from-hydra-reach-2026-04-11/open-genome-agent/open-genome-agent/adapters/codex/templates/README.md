# Codex adapter templates

These files are rendered into:

- `AGENTS.md`
- `.agents/skills/*`
- `.codex/config.toml`
- `.codex/agents/*`
- `.codex/hooks.json`

The generated runtime bundle lives under `adapters/codex/dist/`.
