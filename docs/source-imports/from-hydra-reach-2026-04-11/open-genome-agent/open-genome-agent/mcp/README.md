# MCP stubs

These are local-first placeholders for future MCP servers.

They are intentionally minimal in the scaffold. The goal is to define interfaces early, then harden them once the core VCF-first workflow is stable.
