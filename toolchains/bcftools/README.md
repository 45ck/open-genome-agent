# bcftools

Variant and VCF/BCF querying, filtering, and transformation.

## Expectation

Pin versions in your workstation report and capture every invoked command in `commands.jsonl`.

## MVP role

This toolchain note exists so skills and agents can reference a stable expectation even before the wrapper scripts are fully implemented.
